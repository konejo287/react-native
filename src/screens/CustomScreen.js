import React from 'react';
import { Text, StyleSheet } from 'react-native';

const CustomScreen = () => {
  return <Text >mario custom</Text>;
};

const styles = StyleSheet.create({
  greeting: {
    fontSize: 45
  }
});

export default CustomScreen;